baseurl: ""
url: "http://127.0.0.1:4000"

# Local-only override for root-path preview:
# bundle exec jekyll serve --config _config.yml,_config_local.yml
#
# Do not commit a changed baseurl in _config.yml. GitHub Pages serves this
# project site under /me/.
